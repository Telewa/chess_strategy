pipeline {
	agent any
	stages {
        stage('Build Backend') {
            steps {
                sh 'docker build -t chesser-backend:1.0.0 backend/chesser'
            }
        }

        stage('Sanity Check') {
            steps {
                sh 'docker images | grep chesser-backend'
            }
        }
    }
}