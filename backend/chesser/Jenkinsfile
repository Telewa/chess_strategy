pipeline {
	agent {
	    dockerfile {
	        dir 'backend/chesser'
	        args '-t chesser-backend'
	    }
	}
	stages {
        stage('Build') {
            steps {
				sh 'cd backend/chesser && docker build -t chesser-backend .'
            }
        }
    }
}