pipeline {
	agent {
	    dockerfile {
	        dir 'backend/chesser'
	    }
	}
	stages {
        stage('Build') {
            steps {
				sh 'cd backend/chesser && ls -la '
            }
        }
    }
}