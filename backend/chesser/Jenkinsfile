pipeline {
	agent any
	stages {
        stage('Build') {
            steps {
                withEnv(["PATH=$PATH:~/.local/bin"]){
				    sh 'ls -la'
				    sh 'docker images'
				}
            }
        }
    }
}