pipeline {
	agent any
	stages {
        stage('Build') {
            steps {
                withEnv(["PATH=$PATH:~/.local/bin"]){
				    sh 'ls -la'
				    sh 'cd backend/chesser && docker build -t chesser:1.0.0 . && docker images | grep chess'
				}
            }
        }
    }
}