pipeline {
	agent {
	    docker {
	        image 'openjdk:11-jdk'
	    }
	}
	stages {
        stage('Build') {
            steps {
				sh 'cd backend/chesser && ls -la '
            }
        }
    }
}